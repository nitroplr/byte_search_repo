name: byte_search
description: High-performance, low-allocation byte searching utilities for Uint8List, optimized for repeated scans and high-reject workloads.
version: 0.1.0
homepage: https://github.com/nitroplr/byte_search_repo
repository: https://github.com/nitroplr/byte_search_repo
issue_tracker: https://github.com/nitroplr/byte_search_repo/issues

topics:
  - performance
  - contains
  - bytes
  - parsing
  - utilities

environment:
  sdk: ">=3.10.4 <4.0.0"

dependencies:
# no runtime deps

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
