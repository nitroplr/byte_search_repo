name: byte_search
description: "Fast byte-level searching utilities for Uint8List. Includes compiled BytePattern (BMH/Horspool) and ByteSet for high-reject workloads."
version: 0.1.0
homepage: https://github.com/nitroplr/byte_search_repo
repository: https://github.com/nitroplr/byte_search_repo
issue_tracker: https://github.com/nitroplr/byte_search_repo/issues
topics:
  - bytes
  - search
  - performance
  - algorithms
  - parsing

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
# no runtime deps

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
