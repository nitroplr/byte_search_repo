## 0.1.0

- Initial release.
    - `RecordReader` for delimiter-separated record extraction with bounded scanning
    - `ChunkedFileReader` for streaming large files and file subranges
    - `BinarySearchFile` for binary searching sorted record files by parsed key
    - Re-exports `byte_search` for byte-level search primitives
