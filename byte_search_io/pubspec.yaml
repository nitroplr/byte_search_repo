name: byte_search_io
description: Low-allocation building blocks for reading large delimiter-separated files with RandomAccessFile (chunking, record extraction, binary search).
version: 0.1.0
homepage: https://github.com/nitroplr/byte_search_repo
repository: https://github.com/nitroplr/byte_search_repo
issue_tracker: https://github.com/nitroplr/byte_search_repo/issues

topics:
  - performance
  - parsing
  - logging
  - io
  - bytes

environment:
  sdk: ">=3.10.4 <4.0.0"

platforms:
  linux:
  macos:
  windows:
  android:
  ios:

dependencies:
  byte_search: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
