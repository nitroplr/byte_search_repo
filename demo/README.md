# Demo & Benchmarks

This directory contains local benchmarks for the `byte_search` package.

`data/` holds a sanitized EverQuest log excerpt used to demonstrate
high-reject byte-search workloads.

Nothing in this directory is published to pub.dev.